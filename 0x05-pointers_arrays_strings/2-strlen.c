#include "main.h"

/**
 * _strlen - Function that returns the length the of a string
 * @s: char parameter
 * Return: integer string of a string
 */i
int _strlen(char *s)
{
	int length;
	length = strlen(s);
	return length;
}
